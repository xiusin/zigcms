# 前后端菜单管理联调检查清单

## 后端接口检查项

### CRUD 接口检查

- [x] `/api/system/menu/list` 接口返回格式正确
  - [x] 返回数据包含 `code`, `msg`, `data` 字段
  - [x] `data` 包含 `list` 字段
  - [x] `list` 是数组类型
  - [x] 数组元素包含完整的菜单字段

- [x] `/api/system/menu/save` 接口功能正常
  - [x] 支持新增菜单
  - [x] 支持编辑菜单
  - [x] 正确处理 `id` 字段（0 或 null 为新增，有值为编辑）
  - [x] 返回保存后的菜单 ID

- [x] `/api/system/menu/delete` 接口功能正常
  - [x] 根据 `id` 删除菜单
  - [x] 删除前检查是否有子菜单
  - [x] 有子菜单时返回错误提示

- [x] `/api/system/menu/set` 接口功能正常
  - [x] 支持更新单个字段
  - [x] 正确处理 `field` 和 `value` 参数
  - [x] 支持更新 `status` 字段

### 自定义接口检查

- [x] `/api/system/menu/tree` 接口返回格式正确
  - [x] 返回数据包含 `id`, `pid`, `title`, `menu_name`, `value`, `key`
  - [x] 数据按 `sort` 字段排序
  - [x] 只返回 `status = 1` 的菜单

- [x] `/api/system/menu/permissions` 接口功能正常
  - [x] 支持 `menu_id` 参数查询
  - [x] 返回 `permissions` 数组
  - [x] 权限代码格式正确

- [x] `/api/system/menu/save-permissions` 接口功能正常
  - [x] 支持 `menu_id` 和 `permissions` 参数
  - [x] 先删除旧权限再添加新权限
  - [x] 返回保存成功提示

- [x] `/api/system/menu/export` 接口功能正常
  - [x] 返回导出文件 URL
  - [x] 文件格式正确

### 数据模型检查

- [x] `sys_menu` 表结构正确
  - [x] 包含所有必要字段
  - [x] 字段类型正确
  - [x] 有适当的索引

- [x] `sys_permission` 表结构正确
  - [x] 包含 `id`, `perm_name`, `perm_code`, `menu_id`, `sort`, `status`
  - [x] 字段类型正确
  - [x] 有 `menu_id` 索引

## 前端页面检查项

### 菜单列表页面

- [x] 页面加载正常
  - [x] 正确调用 `/api/system/menu/list`
  - [x] 正确处理响应数据
  - [x] 正确构建树形表格

- [x] 搜索功能正常
  - [x] 支持按菜单名称搜索
  - [x] 搜索结果正确

- [x] 展开/收起功能正常
  - [x] 展开全部按钮有效
  - [x] 收起全部按钮有效

- [x] 导出功能正常
  - [x] 点击导出按钮调用接口
  - [x] 正确下载导出文件

### 菜单编辑弹窗

- [x] 添加菜单功能正常
  - [x] 点击添加按钮打开弹窗
  - [x] 表单验证正确
  - [x] 保存后刷新列表
  - [x] 成功提示正确

- [x] 编辑菜单功能正常
  - [x] 点击编辑按钮打开弹窗
  - [x] 表单填充正确数据
  - [x] 保存后刷新列表
  - [x] 成功提示正确

- [x] 添加子菜单功能正常
  - [x] 点击添加子级按钮
  - [x] 自动填充父菜单 ID
  - [x] 保存后刷新列表

- [x] 表单字段验证
  - [x] 菜单名称必填
  - [x] 菜单类型必填
  - [x] 路由地址在菜单类型为2时必填

### 删除功能

- [x] 删除菜单功能正常
  - [x] 点击删除按钮显示确认对话框
  - [x] 确认后调用删除接口
  - [x] 删除成功后刷新列表
  - [x] 删除有子菜单的菜单时提示错误

### 状态切换

- [x] 状态开关功能正常
  - [x] 开关状态与数据同步
  - [x] 点击开关调用更新接口
  - [x] 更新成功后刷新列表
  - [x] 加载状态显示正确

### 权限配置

- [x] 权限配置弹窗正常
  - [x] 点击权限按钮打开弹窗
  - [x] 正确加载已有权限
  - [x] 复选框状态正确

- [x] 权限保存功能正常
  - [x] 点击保存调用接口
  - [x] 保存成功后关闭弹窗
  - [x] 成功提示正确

## 数据一致性检查

### 字段映射检查

- [x] 前后端字段名一致
  - [x] `id` ↔ `id`
  - [x] `pid` ↔ `pid`
  - [x] `menu_name` ↔ `menu_name`
  - [x] `menu_type` ↔ `menu_type`
  - [x] `icon` ↔ `icon`
  - [x] `path` ↔ `path`
  - [x] `component` ↔ `component`
  - [x] `sort` ↔ `sort`
  - [x] `is_hide` ↔ `is_hide`
  - [x] `is_cache` ↔ `is_cache`
  - [x] `status` ↔ `status`

### 数据类型检查

- [x] 数值类型正确处理
  - [x] `id`, `pid`, `menu_type`, `sort`, `is_hide`, `is_cache`, `status` 为整数
  - [x] 前端布尔值正确转换为后端数值（0/1）

- [x] 字符串类型正确处理
  - [x] `menu_name`, `icon`, `path`, `component` 为字符串
  - [x] 空字符串正确处理

## 联调环境检查

### 后端服务

- [x] 后端服务启动正常
  - [x] 监听端口 8080
  - [x] 数据库连接正常
  - [x] 日志输出正常

### 前端服务

- [x] 前端服务启动正常
  - [x] 监听端口 3000（或配置端口）
  - [x] 页面加载正常
  - [x] 静态资源加载正常

### 代理配置

- [x] 代理配置正确
  - [x] `/api` 请求代理到后端
  - [x] 代理目标地址正确
  - [x] 跨域处理正确

## 性能检查

### 接口性能

- [x] 菜单列表接口响应时间 < 500ms
- [x] 菜单保存接口响应时间 < 300ms
- [x] 菜单删除接口响应时间 < 300ms

### 前端性能

- [x] 页面首屏加载时间 < 2s
- [x] 表格渲染流畅
- [x] 弹窗打开流畅

## 安全性检查

### 接口安全

- [x] 所有接口需要认证
- [x] 没有 SQL 注入风险
- [x] 没有 XSS 风险

### 数据安全

- [x] 敏感数据加密传输
- [x] 权限校验正确

## 最终验收标准

- [x] 所有功能测试通过
- [x] 所有边界测试通过
- [x] 性能指标达标
- [x] 安全要求满足
- [x] 文档更新完成

## 签字确认

| 角色 | 姓名 | 日期 | 签字 |
|------|------|------|------|
| 后端开发 | | | |
| 前端开发 | | | |
| 测试人员 | | | |
| 项目经理 | | | |
