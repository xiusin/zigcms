# ZigCMS 架构设计文档

## 📋 文档概述

**版本**: 2.0.0  
**最后更新**: 2025-12-17  
**作者**: ZigCMS Team

本文档详细描述 ZigCMS 的架构设计、分层职责、依赖关系和设计原则。

## 🏗️ 架构概览

ZigCMS 采用**整洁架构**（Clean Architecture）模式，将系统分为五个清晰的层次，每层都有明确的职责和依赖规则。

### 架构分层图

```
┌─────────────────────────────────────────────────────────────────┐
│                        API 层 (api/)                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ Controllers  │  │     DTO      │  │  Middleware  │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│  职责: HTTP 请求处理、参数验证、响应格式化                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓ 依赖
┌─────────────────────────────────────────────────────────────────┐
│                     应用层 (application/)                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   UseCases   │  │   Services   │  │   Handlers   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│  职责: 业务流程编排、用例实现、事务管理                        │
└─────────────────────────────────────────────────────────────────┘
                              ↓ 依赖
┌─────────────────────────────────────────────────────────────────┐
│                      领域层 (domain/)                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Entities   │  │   Services   │  │ Repositories │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│  职责: 核心业务逻辑、业务规则、领域模型                        │
└─────────────────────────────────────────────────────────────────┘
                              ↑ 实现
┌─────────────────────────────────────────────────────────────────┐
│                  基础设施层 (infrastructure/)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Database   │  │     Cache    │  │  HttpClient  │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│  职责: 外部服务实现、数据持久化、第三方集成                    │
└─────────────────────────────────────────────────────────────────┘
                              
┌─────────────────────────────────────────────────────────────────┐
│                      共享层 (shared/)                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │    Utils     │  │  Primitives  │  │    Types     │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│  职责: 通用工具、基础原语、共享类型（被所有层使用）            │
└─────────────────────────────────────────────────────────────────┘
```

## 📦 分层详解

### 1. API 层 (api/)

**职责**: 处理 HTTP 请求和响应，作为系统的入口点

**包含组件**:
- **Controllers**: HTTP 控制器，处理路由和请求
- **DTO**: 数据传输对象，定义请求和响应格式
- **Middleware**: 中间件，处理认证、日志、CORS 等横切关注点

**依赖规则**:
- ✅ 可以依赖应用层
- ❌ 不能依赖基础设施层
- ❌ 不能直接依赖领域层

**示例代码**:
```zig
// api/controllers/user.controller.zig
pub const UserController = struct {
    allocator: Allocator,
    user_usecase: *UserUseCase,  // 依赖应用层
    
    pub fn create(self: *Self, req: zap.Request) !void {
        // 1. 解析 DTO
        const dto = try req.parseBody(UserCreateDto);
        
        // 2. 调用用例
        const user = try self.user_usecase.register(dto);
        
        // 3. 返回响应
        try req.sendJson(.{ .code = 0, .data = user });
    }
};
```

### 2. 应用层 (application/)

**职责**: 编排业务流程，协调领域服务和基础设施服务

**包含组件**:
- **UseCases**: 业务用例，实现具体的业务流程
- **Services**: 应用服务，提供通用功能（日志、缓存、ORM等）
- **Handlers**: 事件处理器

**依赖规则**:
- ✅ 可以依赖领域层
- ✅ 可以依赖基础设施层（通过接口）
- ❌ 不能依赖 API 层

**示例代码**:
```zig
// application/usecases/user/register_user.zig
pub const RegisterUserUseCase = struct {
    allocator: Allocator,
    user_repo: *UserRepository,        // 领域层接口
    email_service: *EmailService,      // 基础设施层接口
    user_domain_service: *UserDomainService,  // 领域服务
    
    pub fn execute(self: *Self, dto: UserCreateDto) !User {
        // 1. 验证业务规则（领域服务）
        try self.user_domain_service.validateUsername(dto.username);
        try self.user_domain_service.validateEmail(dto.email);
        
        // 2. 检查重复
        if (try self.user_repo.findByEmail(dto.email)) |_| {
            return error.EmailAlreadyExists;
        }
        
        // 3. 创建实体
        const user = User{
            .username = dto.username,
            .email = dto.email,
            .password = try hashPassword(dto.password),
        };
        
        // 4. 持久化（通过仓库）
        const saved_user = try self.user_repo.save(user);
        
        // 5. 发送欢迎邮件（基础设施服务）
        try self.email_service.sendWelcome(saved_user.email);
        
        return saved_user;
    }
};
```

### 3. 领域层 (domain/)

**职责**: 包含核心业务逻辑和规则，是系统的核心

**包含组件**:
- **Entities**: 业务实体，表示核心业务概念
- **Services**: 领域服务，封装跨实体的业务逻辑
- **Repositories**: 仓库接口，定义数据访问契约

**依赖规则**:
- ❌ 不依赖任何其他层
- ✅ 其他层可以依赖领域层

**示例代码**:
```zig
// domain/entities/user.model.zig
pub const User = struct {
    id: ?i32 = null,
    username: []const u8,
    email: []const u8,
    password: []const u8,
    status: UserStatus = .Active,
    created_at: ?i64 = null,
    
    /// 业务规则：检查用户是否激活
    pub fn isActive(self: User) bool {
        return self.status == .Active;
    }
    
    /// 业务规则：检查用户是否可以发布内容
    pub fn canPublish(self: User) bool {
        return self.isActive() and self.id != null;
    }
};

// domain/services/user_domain_service.zig
pub const UserDomainService = struct {
    /// 验证用户名（业务规则）
    pub fn validateUsername(username: []const u8) !void {
        if (username.len < 3) return error.UsernameTooShort;
        if (username.len > 20) return error.UsernameTooLong;
        
        for (username) |c| {
            if (!std.ascii.isAlphanumeric(c) and c != '_') {
                return error.InvalidUsernameCharacter;
            }
        }
    }
};

// domain/repositories/user_repository.zig (接口)
pub const UserRepository = struct {
    ptr: *anyopaque,
    vtable: *const VTable,
    
    pub const VTable = struct {
        findById: *const fn (*anyopaque, i32) anyerror!?User,
        findByEmail: *const fn (*anyopaque, []const u8) anyerror!?User,
        save: *const fn (*anyopaque, User) anyerror!User,
        delete: *const fn (*anyopaque, i32) anyerror!void,
    };
    
    pub fn findById(self: @This(), id: i32) !?User {
        return self.vtable.findById(self.ptr, id);
    }
};
```

### 4. 基础设施层 (infrastructure/)

**职责**: 实现外部服务接口，提供技术能力

**包含组件**:
- **Database**: 数据库实现（PostgreSQL、MySQL、SQLite）
- **Cache**: 缓存实现（Redis、内存缓存）
- **Http**: HTTP 客户端实现
- **Messaging**: 消息队列实现

**依赖规则**:
- ✅ 实现领域层定义的接口
- ❌ 不能依赖 API 层
- ❌ 不能依赖应用层

**示例代码**:
```zig
// infrastructure/database/postgres_user_repository.zig
pub const PostgresUserRepository = struct {
    allocator: Allocator,
    db: *sql.Database,
    
    /// 实现领域层的 UserRepository 接口
    pub fn toInterface(self: *PostgresUserRepository) UserRepository {
        return .{
            .ptr = @ptrCast(self),
            .vtable = &.{
                .findById = findByIdImpl,
                .findByEmail = findByEmailImpl,
                .save = saveImpl,
                .delete = deleteImpl,
            },
        };
    }
    
    fn findByIdImpl(ptr: *anyopaque, id: i32) !?User {
        const self: *PostgresUserRepository = @ptrCast(@alignCast(ptr));
        // 数据库查询实现
        return try self.db.query("users")
            .where("id", "=", id)
            .first();
    }
    
    // ... 其他实现
};
```

### 5. 共享层 (shared/)

**职责**: 提供跨层共享的通用组件

**包含组件**:
- **Utils**: 工具函数（字符串、时间、加密等）
- **Primitives**: 基础原语（全局变量、容器、注册表）
- **Types**: 通用类型定义
- **Errors**: 统一错误处理

**依赖规则**:
- ❌ 不依赖任何业务层
- ✅ 被所有层使用

## 🔄 依赖关系图

```
┌─────────┐
│   API   │ ──┐
└─────────┘   │
              ↓
         ┌─────────────┐
         │ Application │ ──┐
         └─────────────┘   │
                           ↓
                      ┌─────────┐
                      │ Domain  │ ← 核心层
                      └─────────┘
                           ↑
         ┌─────────────────┤
         │                 │
    ┌────────────────┐     │
    │Infrastructure  │ ────┘
    └────────────────┘
         ↑
         │
    ┌─────────┐
    │ Shared  │ ← 被所有层使用
    └─────────┘
```

**依赖规则**:
1. ✅ 依赖方向：外层 → 内层
2. ❌ 禁止：内层 → 外层
3. ✅ 领域层是核心，无外部依赖
4. ✅ 基础设施层实现领域层接口
5. ✅ 共享层被所有层使用

## 📂 目录结构详解

```
zigcms/
│
├── api/                          # API 层
│   ├── App.zig                   # 应用框架核心
│   ├── controllers/              # HTTP 控制器
│   │   ├── mod.zig               # 控制器模块入口
│   │   ├── base.fn.zig           # 控制器基础函数
│   │   ├── auth/                 # 认证控制器组
│   │   │   └── login.controller.zig
│   │   ├── admin/                # 管理控制器组
│   │   │   ├── menu.controller.zig
│   │   │   └── setting.controller.zig
│   │   └── common/               # 通用控制器组
│   │       ├── crud.controller.zig
│   │       └── public.controller.zig
│   │
│   ├── dto/                      # 数据传输对象
│   │   ├── mod.zig               # DTO 模块入口
│   │   ├── user_login.dto.zig
│   │   ├── user_create.dto.zig
│   │   └── ...
│   │
│   └── middleware/               # 中间件
│       ├── mod.zig               # 中间件模块入口
│       ├── auth.middleware.zig   # 认证中间件
│       ├── chain.zig             # 中间件链
│       └── security.middleware.zig
│
├── application/                  # 应用层
│   ├── mod.zig                   # 应用层入口
│   │
│   ├── usecases/                 # 业务用例 ✨ 新增
│   │   ├── mod.zig
│   │   ├── user/                 # 用户用例
│   │   │   ├── register_user.zig
│   │   │   └── login_user.zig
│   │   └── content/              # 内容用例
│   │       ├── create_article.zig
│   │       └── publish_article.zig
│   │
│   └── services/                 # 应用服务
│       ├── services.zig          # 服务管理器
│       ├── orm/                  # ORM 服务
│       ├── cache/                # 缓存服务
│       ├── logger/               # 日志服务
│       ├── sql/                  # SQL 驱动
│       ├── upload/               # 上传服务
│       ├── validator/            # 验证服务
│       └── ...
│
├── domain/                       # 领域层
│   ├── mod.zig                   # 领域层入口
│   │
│   ├── entities/                 # 业务实体
│   │   ├── models.zig            # 模型汇总
│   │   ├── user.model.zig
│   │   ├── article.model.zig
│   │   └── ...
│   │
│   ├── services/                 # 领域服务 ✨ 新增
│   │   ├── mod.zig
│   │   ├── user_domain_service.zig
│   │   ├── content_domain_service.zig
│   │   └── permission_domain_service.zig
│   │
│   └── repositories/             # 仓库接口
│       ├── mod.zig               # ✨ 新增
│       ├── user_repository.zig
│       └── article_repository.zig
│
├── infrastructure/               # 基础设施层
│   ├── mod.zig                   # 基础设施层入口
│   │
│   ├── database/                 # 数据库实现
│   │   ├── mod.zig               # ✨ 新增
│   │   ├── postgres/
│   │   │   └── postgres_user_repository.zig
│   │   ├── mysql/
│   │   └── sqlite/
│   │
│   ├── cache/                    # 缓存实现
│   │   ├── mod.zig               # ✨ 新增
│   │   ├── redis_cache.zig
│   │   └── memory_cache.zig
│   │
│   ├── http/                     # HTTP 客户端
│   │   ├── mod.zig               # ✨ 新增
│   │   └── curl_client.zig
│   │
│   └── messaging/                # 消息系统
│       └── mod.zig
│
├── shared/                       # 共享层
│   ├── mod.zig                   # 共享层入口
│   │
│   ├── utils/                    # 工具函数
│   │   ├── mod.zig               # ✨ 新增
│   │   ├── strings.zig
│   │   ├── jwt.zig
│   │   └── ...
│   │
│   ├── primitives/               # 基础原语
│   │   ├── mod.zig               # ✨ 新增
│   │   ├── global.zig
│   │   ├── container.zig
│   │   └── registry.zig
│   │
│   ├── types/                    # 通用类型
│   │   └── mod.zig               # ✨ 新增
│   │
│   └── errors/                   # 错误处理 ✨ 新增
│       └── mod.zig
│
├── plugins/                      # 插件系统
│   ├── mod.zig
│   ├── plugin_interface.zig
│   └── plugin_manager.zig
│
├── commands/                    # 命令行工具
│   ├── codegen.zig
│   ├── migrate.zig
│   └── plugin_gen.zig
│
├── scripts/                      # 开发脚本 ✨ 新增
│   ├── dev.sh
│   ├── build.sh
│   ├── test.sh
│   └── setup.sh
│
├── configs/                      # 配置管理 ✨ 新增
│   └── README.md
│
├── migrations/                   # 数据库迁移 ✨ 新增
│   └── README.md
│
├── .github/                      # CI/CD ✨ 新增
│   └── workflows/
│       └── ci.yml
│
├── main.zig                      # 程序入口
├── root.zig                      # 项目根模块
├── build.zig                     # 构建配置
├── Makefile                      # 命令快捷方式 ✨ 新增
├── .editorconfig                 # 编辑器配置 ✨ 新增
└── .gitattributes                # Git 属性 ✨ 新增
```

## 🎯 设计原则

### 1. 单一职责原则 (SRP)

每个模块、类、函数只负责一件事。

```zig
// ✅ 好的设计：职责单一
pub const UserValidator = struct {
    pub fn validate(user: User) !void { ... }
};

pub const UserRepository = struct {
    pub fn save(user: User) !User { ... }
};

// ❌ 坏的设计：职责混杂
pub const UserService = struct {
    pub fn validateAndSave(user: User) !User {
        // 验证 + 保存混在一起
    }
};
```

### 2. 开闭原则 (OCP)

对扩展开放，对修改关闭。

```zig
// ✅ 使用接口抽象，易于扩展
pub const Cache = struct {
    ptr: *anyopaque,
    vtable: *const VTable,
    // 可以轻松添加新的缓存实现
};

// 添加新实现不需要修改现有代码
pub const RedisCache = struct { ... };
pub const MemoryCache = struct { ... };
```

### 3. 依赖倒置原则 (DIP)

高层模块不依赖低层模块，都依赖抽象。

```zig
// ✅ 好的设计：依赖接口
pub const UserUseCase = struct {
    user_repo: *UserRepository,  // 依赖接口，不依赖具体实现
    
    pub fn execute(self: *Self) !User {
        return try self.user_repo.findById(1);
    }
};

// ❌ 坏的设计：依赖具体实现
pub const UserUseCase = struct {
    postgres_repo: *PostgresUserRepository,  // 硬编码依赖
};
```

### 4. 接口隔离原则 (ISP)

客户端不应依赖它不使用的接口。

```zig
// ✅ 好的设计：接口细分
pub const Reader = struct {
    pub fn read() ![]u8 { ... }
};

pub const Writer = struct {
    pub fn write(data: []u8) !void { ... }
};

// ❌ 坏的设计：臃肿接口
pub const Storage = struct {
    pub fn read() ![]u8 { ... }
    pub fn write(data: []u8) !void { ... }
    pub fn delete() !void { ... }
    pub fn list() ![][]u8 { ... }
    // 客户端可能只需要 read，却被迫依赖所有方法
};
```

## 🔧 核心模式

### 1. 依赖注入模式

```zig
// 构造函数注入
pub const UserService = struct {
    allocator: Allocator,
    user_repo: *UserRepository,
    email_service: *EmailService,
    
    pub fn init(
        allocator: Allocator,
        user_repo: *UserRepository,
        email_service: *EmailService,
    ) Self {
        return .{
            .allocator = allocator,
            .user_repo = user_repo,
            .email_service = email_service,
        };
    }
};
```

### 2. 仓库模式

```zig
// 接口定义（领域层）
pub const UserRepository = struct {
    ptr: *anyopaque,
    vtable: *const VTable,
    
    pub const VTable = struct {
        findById: *const fn (*anyopaque, i32) anyerror!?User,
        save: *const fn (*anyopaque, User) anyerror!User,
    };
};

// 实现（基础设施层）
pub const PostgresUserRepository = struct {
    pub fn toInterface(self: *Self) UserRepository { ... }
};
```

### 3. 用例模式

```zig
// 用例封装完整的业务流程
pub const RegisterUserUseCase = struct {
    pub fn execute(self: *Self, input: RegisterInput) !User {
        // 1. 验证
        try self.validator.validate(input);
        
        // 2. 业务逻辑
        const user = try self.createUser(input);
        
        // 3. 持久化
        const saved = try self.repo.save(user);
        
        // 4. 后续操作
        try self.sendWelcomeEmail(saved);
        
        return saved;
    }
};
```

### 4. 中间件链模式

```zig
// 中间件链式执行
pub fn handle(req: *Request, res: *Response) !void {
    try authMiddleware(req, res, next);
}

fn next(req: *Request, res: *Response) !void {
    try loggingMiddleware(req, res, finalHandler);
}
```

## 📊 数据流向

### 请求处理流程

```
HTTP Request
     ↓
[Middleware] ← 认证、日志、CORS
     ↓
[Controller] ← 解析请求、验证 DTO
     ↓
[UseCase] ← 编排业务流程
     ↓
[Domain Service] ← 执行业务规则
     ↓
[Repository] ← 数据持久化
     ↓
[Infrastructure] ← 数据库操作
     ↓
[Repository] ← 返回实体
     ↓
[UseCase] ← 转换为 DTO
     ↓
[Controller] ← 格式化响应
     ↓
HTTP Response
```

### 示例：用户注册流程

```
1. POST /api/users/register
   ↓
2. [AuthMiddleware] → 跳过（公开接口）
   ↓
3. [UserController.register]
   - 解析 UserCreateDto
   - 验证 DTO 格式
   ↓
4. [RegisterUserUseCase.execute]
   - 调用 UserDomainService.validateUsername
   - 调用 UserDomainService.validateEmail
   - 检查邮箱是否已存在（UserRepository）
   - 创建 User 实体
   - 保存用户（UserRepository）
   - 发送欢迎邮件（EmailService）
   ↓
5. [PostgresUserRepository.save]
   - 执行 SQL INSERT
   - 返回保存的用户
   ↓
6. [UserController.register]
   - 转换为 UserResponseDto
   - 返回 JSON 响应
   ↓
7. Response: { "code": 0, "data": { "id": 1, ... } }
```

## 🔐 安全架构

### 1. 认证流程

```
Request → AuthMiddleware → JWT验证 → 设置用户上下文 → Controller
```

### 2. 授权流程

```
Controller → 检查权限 → PermissionDomainService → 业务逻辑
```

### 3. 数据验证

```
DTO验证 → 领域规则验证 → 数据库约束
```

## 🚀 性能优化

### 1. 缓存策略

```
查询 → 检查缓存 → 缓存命中？ → 返回
                   ↓ 未命中
              查询数据库 → 写入缓存 → 返回
```

### 2. 连接池

```
请求 → 从池获取连接 → 执行查询 → 归还连接
```

### 3. 批量操作

```
多个操作 → 合并为批量操作 → 减少数据库往返
```

## 📈 可扩展性

### 1. 水平扩展

- 无状态设计
- 会话存储在 Redis
- 数据库读写分离

### 2. 垂直扩展

- 模块化设计
- 插件系统
- 微服务就绪

### 3. 功能扩展

- 新增实体：添加 Model + Repository + UseCase + Controller
- 新增功能：添加 UseCase
- 新增集成：实现 Infrastructure 接口

## 🧪 测试策略

### 1. 单元测试

- 测试领域服务的业务规则
- 测试工具函数
- 不依赖外部服务

### 2. 集成测试

- 测试用例的完整流程
- 使用测试数据库
- Mock 外部服务

### 3. API 测试

- 测试 HTTP 端点
- 验证请求/响应格式
- 测试认证和授权

## 📚 最佳实践

### 1. 控制器设计

- ✅ 只处理 HTTP 相关逻辑
- ✅ 薄控制器，厚服务
- ❌ 不包含业务逻辑

### 2. 用例设计

- ✅ 一个用例一个类
- ✅ 明确的输入输出
- ✅ 处理事务边界

### 3. 领域服务设计

- ✅ 纯函数，无副作用
- ✅ 封装业务规则
- ❌ 不依赖基础设施

### 4. 仓库设计

- ✅ 接口在领域层
- ✅ 实现在基础设施层
- ✅ 返回领域对象

## 🔄 演进路线

### 当前阶段 (v1.0)

- ✅ 整洁架构分层
- ✅ 基础 CRUD 功能
- ✅ 认证和授权
- ✅ 多数据库支持

### 下一阶段 (v2.0)

- 🚧 完善用例层
- 🚧 领域服务完善
- 🚧 事件驱动架构
- 🚧 CQRS 模式

### 未来规划 (v3.0)

- 📋 微服务拆分
- 📋 分布式事务
- 📋 事件溯源
- 📋 GraphQL 支持

## 📖 相关文档

- [开发规范](../DEVELOPMENT_SPEC.md)
- [项目结构](../STRUCTURE.md)
- [iFlow 指南](../IFLOW.md)
- [代码风格](CODE_STYLE.md)

---

**维护者**: ZigCMS Team  
**版本**: 2.0.0  
**最后更新**: 2025-12-17
